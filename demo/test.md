在 `/Users/lelouch/chat` 这个路径下，opencode 有一个刚发生的会话，标题是"今天日期查询"。对话模型是 `axonhub-openai` 的 `GPT-5.2`。我问他图片里的文字是什么，然后模型接着调用一个 `question`，自称已经回答了图片文字内容。

但在主对话界面里，我根本看不到模型“回答”，只有连续 `question` 调用，也没有在 `question` 里面看到它的回答。图片是对话截图，你可以看到 `question` 调用。

请你认真调研一下，这是模型的问题 (它确实没有回答)，还是 `opencode` 渲染显示回答的问题 (回答了但没有显示)？